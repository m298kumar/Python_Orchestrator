"""
Shared Conftest for API Tests
Auto-generated by STLC Platform — Stage 3 API Test Generator.

API: {{ spec_title }}
Base URL: {{ base_url }}
"""

import pytest
import requests


@pytest.fixture
def base_url():
    """Base URL for the API under test."""
    return "{{ base_url }}"


@pytest.fixture
def auth_headers():
    """Authorization headers. Replace <TOKEN> with a real token."""
    # TODO: Replace with actual authentication logic
    return {"Authorization": "Bearer <TOKEN>"}


@pytest.fixture
def api_session(base_url):
    """Requests session pre-configured with the base URL."""
    session = requests.Session()
    session.headers.update({"Content-Type": "application/json"})
    return session
