/**
 * Step Definitions (Cucumber.js)
 * ===============================
 * Auto-generated by STLC Platform Stage 2.
 * Each step body throws an error -- fill in the implementation.
 */

const { Given, When, Then } = require('@cucumber/cucumber');
{% if automation_lib == "playwright" %}
// const { chromium } = require('playwright');  // uncomment when implementing
{% elif automation_lib == "selenium" %}
// const { Builder, By } = require('selenium-webdriver');  // uncomment when implementing
{% endif %}


// -- Given Steps --

{% for step in given_steps %}
Given('{{ step.cucumber_pattern }}', async function ({% for p in step.param_names %}{{ p }}{% if not loop.last %}, {% endif %}{% endfor %}) {
    // {{ step.pattern }}
    throw new Error('TODO: implement this step');
});

{% endfor %}

// -- When Steps --

{% for step in when_steps %}
When('{{ step.cucumber_pattern }}', async function ({% for p in step.param_names %}{{ p }}{% if not loop.last %}, {% endif %}{% endfor %}) {
    // {{ step.pattern }}
    throw new Error('TODO: implement this step');
});

{% endfor %}

// -- Then Steps --

{% for step in then_steps %}
Then('{{ step.cucumber_pattern }}', async function ({% for p in step.param_names %}{{ p }}{% if not loop.last %}, {% endif %}{% endfor %}) {
    // {{ step.pattern }}
    throw new Error('TODO: implement this step');
});

{% endfor %}
